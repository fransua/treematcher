To test:

python treematcher.py

## To Do
- @.species is "sapiens" or "pygmaeus" is the same as @.species == "sapiens"or node.name=="pygmaeus" which may not be what people expect
- @.species will fail if not all nodes have species
- If someone were to name their leaves with keywords, they would be modified eed to replace inner strings with temporary variables during the replace